server:
  port: 8189
  servlet:
    context-path: /scheduler

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/scheduler?&useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: root

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        ddl-auto: validate

# spprtscheduler@gmail.com
# iRIIYtpu6e1&
  mail:
    host: smtp.gmail.com
#    host: smtp.mail.ru
    port: 587
#    port: 465
    username: spprtscheduler
#    username: scheduler@internet.ru
    password: cxlqdyrubemqswzn
#    password: WmfjcKa8aSvYvkhQBm0H
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          connectiontimeout: 5000
          timeout: 3000
          auth: true
          starttls:
            enable: true

mail:
  name: spprtscheduler@gmail.com
#  name: scheduler@internet.ru
  debug: true

app:
  debug: true

# определение файла для логирования при запуске
#$ java -Dlogging.file=/home/scheduler/app/logs/app.log -jar scheduler-0.0.1-SNAPSHOT.jar
logging:
  level:
    ru:
      team:
        scheduler:
          oapi: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logFile.log